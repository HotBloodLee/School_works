日志记录1：2020.2.23（Q_001）
	ImageView中在测试时一直出现原背景与剪切背景同时出现的情况，百度搜索方法无果
		判断：图片的加载方式有问题
		具体原因：在xml中的ImageView标签里面设置了所用图片，去掉后解决问题Q_001
	（Q_002）
	源程序中出现图片切割后定位不准的问题，先认为是createBitmap函数里面单位不是px，导致宽度，高度不对，尝试修改数字但是无果，百度后显示单位就是px，后改为用变量a、b分别存储big的宽和高，用a/8替换120，b/4替换150，成功
		判断：big的大小不是960x600，不能使用原像素的数据
		具体原因：未查明（Q_002）